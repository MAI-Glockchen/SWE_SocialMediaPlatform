version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./sqlite:/app/sqlite
    environment:
      # nothing needed unless you add config
      PYTHONUNBUFFERED: "1"
